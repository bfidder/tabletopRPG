<h1>Chat</h1>

<ul id="chat">
	<% if not @messages.nil? %>
		<%= render @messages %>
	<% end %>
</ul>

<%=form_for Message.new, :remote => true do |f| %>
	<%= f.text_field :content %>
	<%= f.submit "Send" %>
<% end %>
